%import from fun folder
import fun.fibonacciOpt


%fibonacci example
f = @(x) (6 * x - 2) * (6 * x - 2) * sin(12 * x - 4);
bounds = [0 1];
eps = 0.001;
opt = fibonacciOpt(f, bounds, eps);

figure
ezplot(f)
xlim(bounds);